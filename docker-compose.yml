services:
  web:
    image: nginx:alpine
    ports:
      - "20008:80"
    volumes:
      - ./site:/usr/share/nginx/html
    deploy:
      restart_policy:
        condition: any
    networks:
      - app_network

networks:
  app_network:
    driver: overlay
